# WeeklyMeter 0.6.6 — Security scope

## Before you sign in

WeeklyMeter is an **unofficial, experimental app**, not an OpenAI-endorsed client. It shows a selected Codex weekly quota, not all ChatGPT model limits.

- Passwords are entered on OpenAI's page in your external browser. The app uses a PKCE browser flow with a local callback, without a PC or relay server.
- Sign-in permissions **are broader than read-only usage access**. Tokens are encrypted with Android Keystore and excluded from backups; this is not protection against a compromised device.
- The app sends authentication and usage requests, not chat/model-generation requests. Live-account quota and billing effects have not been measured. The internal usage endpoint or sign-in flow may change.
- The floating widget needs optional **Display over other apps** access. It draws saved usage; it does not read other apps. Screen content may be captured in screenshots or recordings.
- Version 0.6.6 passed its APK build and host regression checks. **Real-account login, actual Keystore behavior, S25 Ultra / One UI, power-saving reliability, and current-version Android UI execution remain unverified.** See [test scope](TEST-RESULTS.txt).

Never post passwords, tokens, authorization codes, callback URLs, or account files in public issues. Redact screenshots before sharing. Disconnect removes locally saved account data; revoking server access is a separate action.

## 한국어 상세

0.6.6은 표시·편집기의 중복 작업과 로컬 상태 동기화를 개선합니다. 한 번의 직렬 조회 작업에서 연결 복구와 조회가 같은 암호화 저장소 읽기를 공유하지만, 평문 세션을 작업 간에 보관하지 않습니다. 회전 토큰 저장·불확실한 인증 응답 차단·재로그인 보호는 유지합니다. 권한·네트워크 주소·조회 간격은 변경하지 않습니다.

설정 미리보기는 프레임마다 묶어서 그리며 홈 위젯 이미지는 한 번의 게시 안에서만 제한적으로 공유합니다. 플로팅은 로컬 캐시의 만료 시각을 넘으면 이전 비트맵을 재사용하지 않습니다. 화면이 꺼진 동안에는 표시용 타이머를 중단하고 복귀 때 다시 확인합니다. 메인 화면의 이벤트 알림과 이 만료 처리는 추가 사용량 조회가 아니며 OS의 실행 지연을 우회하지 않습니다.

## 인증

외부 브라우저에 auth.openai.com/oauth/authorize를 열고, PKCE S256과 256비트 난수 state를 사용합니다. 요청 scope는 openid profile email offline_access입니다. 공개 Codex 클라이언트 ID를 참고하지만 독립 Android 앱의 공식 승인이나 사용량 읽기 전용 권한을 뜻하지 않습니다.

로그인 시작 전에 127.0.0.1:1455에 소켓을 바인딩합니다. 0.0.0.0 또는 LAN 주소로 공개하지 않습니다. 브라우저 콜백은 http://localhost:1455/auth/callback입니다. 정확한 GET 경로·Host·state를 확인하며 중복 파라미터, 과대/잘못된 요청, 코드와 오류의 혼합을 거부합니다. 잘못된 요청은 기존 대기를 소모하지 않습니다. 대기는 단조 시계 기준 최대 10분이고 요청 읽기도 시간·크기 제한을 둡니다.

콜백 코드·토큰은 페이지·로그·알림에 출력하지 않습니다. 고정 HTML과 CSP, no-store, no-referrer를 사용하고 페이지가 쿼리를 브라우저 기록에서 치웁니다. 완료 페이지는 토큰 저장 성공을 미리 주장하지 않습니다. 브라우저 자체가 관리하는 방문 기록·로그인 세션까지 앱이 통제하는 것은 아닙니다.

PKCE verifier와 state는 메모리에만 있으며 대기 중 프로세스가 종료되면 로그인부터 다시 시작합니다. 저장된 로그인 세션을 삭제하는 동작은 아닙니다. 암호화된 대기 상태를 되살리고 리스너가 없는 척 진행하지 않습니다.

## 네이티브 HTTPS 호출

- POST https://auth.openai.com/oauth/token — 인증 코드 교환 / 갱신
- GET https://chatgpt.com/backend-api/wham/usage — 액세스 토큰 / 계정 ID

정확한 주소·메서드를 강제하고 리디렉션을 따르지 않습니다. 시스템 CA만 신뢰하며 인증서 고정은 하지 않습니다. HTTP 원문 오류나 토큰을 로깅하지 않습니다. 서버의 내부 사용량 경로는 호환성이 바뀔 수 있습니다.

외부 브라우저의 OpenAI 로그인 및 Google 등 로그인 제공자 통신은 별개입니다. 비밀번호 입력 UI, WebView, 쿠키/다른 앱의 세션 추출, 다른 기기의 auth.json 복사, 토큰 붙여넣기는 없습니다.

## 저장 및 지속성

토큰은 noBackupFilesDir에 Android Keystore AES-GCM / AtomicFile로 저장합니다. 백업·기기간 이동은 꺼져 있습니다. 자동 조회를 위해 생체인증을 매번 요구하지 않습니다. JWT 클레임은 HTTPS 응답의 계정/만료 힌트이며 로컬 권한 허용 근거로 사용하지 않습니다.

모든 토큰 교환·저장·로그아웃을 Repo.IO에서 직렬화합니다. 브라우저 대기는 별도 스레드입니다. 인증 POST 후 토큰을 저장하기 전에 작업 취소로 응답을 버리지 않습니다. 저장 실패 때 같은 새 자격증명의 저장을 한 번 더 시도하며 이전 갱신 토큰으로 서버 요청을 반복하지 않습니다. 프로세스 종료나 응답 유실이 서버 회전과 겹치면 재로그인이 필요할 수 있습니다.

연결 표시 환경설정은 권한의 근거가 아닙니다. 새 프로세스/앱/부팅에서 암호화 세션을 읽어 복원합니다. 조회 실패는 기존 토큰을 삭제하지 않습니다. 초기 연결은 액세스 토큰과 갱신 토큰이 모두 있어야 완료합니다.

0.6.5는 토큰 요청의 HTTP 400/401에서 제한된 오류 본문을 읽고 `invalid_grant` 등 미리 정한 코드만 분류합니다. 원문 설명이나 토큰은 저장하지 않습니다. 갱신 권한 만료·철회 또는 전송 후 결과를 확인하지 못한 경우 자동 재사용을 멈추고 **다시 로그인**을 표시합니다. 기존 자격증명을 자동 삭제하지 않으며, 사용자가 새 브라우저 로그인을 마치고 새 자격증명이 안전하게 저장되어야 교체합니다. 일시적인 사용량 조회 연결 장애·5xx는 이 재로그인 상태를 만들지 않습니다. 새 계정 세대 ID는 비밀이 아닌 값으로, 토큰 저장 직후 프로세스가 종료돼도 다음 복구에서 이전 계정의 표시 캐시를 지웁니다. 영구 로그인이나 계정 연결 안전성을 보증하지 않습니다.

인증 변경은 [OpenAI 공식 인증 안내](https://learn.chatgpt.com/docs/auth)의 브라우저 로그인·저장 자격증명 취급과 대조했습니다. 이는 이 비공식 앱의 승인이나 사용량 전용 권한을 뜻하지 않습니다.

사용량·마지막 조회·배경/글자색 등 비밀 아닌 값은 앱 전용 SharedPreferences에 저장합니다. 홈 위젯과 플로팅 위젯의 스타일·조회 효과 설정은 독립적으로 저장하고, 플로팅 크기·위치는 별도 로컬 설정에 둡니다. 스타일은 계정 연결 해제 후에도 유지됩니다. 0.6.5는 일반 메인 화면의 FLAG_SECURE도 제거하며 비밀번호·토큰을 앱 화면에 표시하지 않습니다. 스타일 설정에는 예시 사용량만 표시합니다. 홈 화면·플로팅·메인 화면의 수치는 주변 사람에게 보이며 캡처에 포함될 수 있습니다. 루팅·OS 침해·같은 앱 실행 권한까지 방어하지 않습니다.

## Android 권한과 실행

INTERNET, ACCESS_NETWORK_STATE, RECEIVE_BOOT_COMPLETED, FOREGROUND_SERVICE, FOREGROUND_SERVICE_DATA_SYNC에 더해 0.6.0은 선택적 플로팅 표시용 SYSTEM_ALERT_WINDOW와 FOREGROUND_SERVICE_SPECIAL_USE를 선언합니다. SYSTEM_ALERT_WINDOW는 플로팅을 선택했을 때 시스템 설정에서 사용자가 직접 허용하며 홈 위젯에 필요하지 않습니다. 추가 권한은 인증 범위를 바꾸지 않습니다.

브라우저 로그인과 수동 조회는 완료·취소·시간 만료 때 종료하는 짧은 dataSync foreground service를 사용합니다. 플로팅은 사용자가 연 세션 동안 유지하는 별도의 specialUse foreground service입니다. 0.6.4부터 플로팅에 START_STICKY를 사용해 시스템이 종료한 실행 세션을 Android가 재생성할 수 있게 합니다. 시스템 재생성의 null Intent는 허용하되 권한·종료 상태를 확인하고 즉시 foreground service로 진입한 뒤 잠금 상태에 따라 창을 표시하거나 숨깁니다. 사용자의 닫기는 stopService/stopSelf로 세션을 종료하며 영구적인 자동 실행 설정은 저장하지 않습니다. 부팅 수신자에서 플로팅을 시작하지 않습니다. 강제 종료·제조사 실행 제한을 우회하거나 재생성 시각을 보장하지 않습니다. 정상 주기 조회는 기존 JobScheduler를 공유합니다. [Android START_STICKY](https://developer.android.com/reference/android/app/Service#START_STICKY)

로그인·수동 조회·플로팅 서비스와 스타일 Activity, 위젯/부팅 수신자는 exported=false입니다. 작업 서비스는 BIND_JOB_SERVICE로 보호합니다. PendingIntent는 명시적·immutable입니다. 홈 위젯 첫 탭은 수동 조회 서비스에 직접 연결하며 같은 위젯을 900ms 안에 3회 누르면 플로팅 표시를 요청합니다. 권한이 없으면 앱에서 허용하도록 안내할 뿐 자동으로 권한을 부여하지 않습니다. 연락처·SMS·마이크·접근성·알림 접근·VPN·외부 저장소 권한을 요구하지 않습니다. Android 13 이상에서 별도 알림 권한을 요청하지 않아 서비스 알림이 일반 알림창에 표시되지 않을 수 있으나 foreground service 자체는 시스템의 실행 앱 목록에서 관리됩니다.

수동 요청은 단조 시계 55초에 협력 취소하고, 서비스·알림은 70초 상한을 둡니다. 0.6.5는 연결 8초·읽기 12초 제한에 더해 **HTTP 요청 하나당 전체 30초**의 단조 시계 제한을 둡니다. 토큰 갱신과 사용량 조회는 서로 다른 요청입니다. 요청 대기가 만료되면 Repo.IO를 해제하고 별도의 제한된 작업자에서 연결 종료를 시도합니다. 이미 완전히 받은 토큰 응답은 연결 정리가 늦다는 이유로 버리지 않습니다. 인증 본문을 보내기 시작한 뒤 결과가 불분명하면 저장된 정보를 보존하고 재로그인을 요구합니다.

플랫폼 I/O 자체가 멈춘 경우 강제로 스레드를 죽인다고 보장하지 않습니다. 통신 작업자 1개와 종료 작업자 1개, 각 대기열 1개로 제한하며 기존 통신이 끝나기 전에는 추가 통신을 거절해 회전 요청이나 스레드가 늘어나지 않게 합니다. OS의 실제 타이머 실행도 지연될 수 있습니다. 위젯 게시 실패는 수동 조회의 시작·종료, 로그인 완료 정리, 언어·스타일 저장을 중단시키지 않도록 분리합니다. 메인·앱 시작·부팅·위젯 갱신의 예약 복구와 화면 표시도 각각 격리합니다. 자동 조회 진단에 필요한 게시 실패 정보는 유지합니다.

새 회전 토큰의 파일 저장이 두 번 모두 실패해도 기존 토큰을 다시 사용하지 않도록 별도 비밀 아닌 재로그인 표시를 저장합니다. 저장 장치 자체가 고장 나 환경설정 기록도 실패하면 같은 프로세스의 차단 상태는 유지하지만 프로세스 종료 이후의 지속성까지 보장하지 못합니다.

## 위젯

퍼센트·초기화·마지막 성공 조회·ChatGPT 식별 표시 중 선택한 요소만 표시합니다. 예시와 실제 위젯은 같은 Canvas 렌더러를 사용합니다. 폰트는 라이선스와 함께 APK에 포함하며 원격 폰트 요청은 없습니다. 비트맵 해상도와 크기별 변형 개수를 제한합니다.

0.6.2의 미리보기는 선택한 dp 크기를 기기 배율에 맞춰 표시하며 작은 고정 영역에 맞춰 축소하지 않습니다. 넘치는 부분은 미리보기 안에서 스크롤합니다. 홈 크기는 이 앱의 위젯에 대해 런처가 제공한 정보를 읽으며, 없으면 예상 1×1 / 2×1로 명시합니다. 플로팅 미리보기는 선택값이고 실제 창은 사용 가능한 화면에 맞춰 제한될 수 있습니다. 다른 앱의 내용·화면을 읽지 않습니다. 홈·플로팅 꾸미기는 메인 화면의 같은 단계에서 각각 열며 권한 허용이나 플로팅 표시를 먼저 요구하지 않습니다. 미리보기와 꾸미기는 예시 데이터만 사용하고 인증·네트워크 조회·권한 범위를 바꾸지 않습니다.

전체 불투명도는 배경·글자·로고·조회 효과를 합친 결과에 한 번 적용하며, 기존 배경 불투명도와 별개입니다. 홈·플로팅별로 0~100%를 독립 저장하고 기존 스타일은 100%로 읽습니다. 0.6.5는 실제 렌더 결과의 모든 픽셀이 투명하면 플로팅 창을 제거해 보이지 않는 터치 영역을 남기지 않습니다. 실행 세션과 자동 예약은 유지되며 스타일·데이터 변경으로 출력이 생기면 다시 표시합니다. 앱 첫 화면에서 표시/숨김 상태를 보고 직접 닫을 수 있으므로 알림 권한에 의존하지 않습니다.

알려진 초기화 시각, 최대 7일 캐시, 시계 역행 검사에서 만료되면 다음 렌더 때 —%로 표시합니다. 즉시성은 보장하지 않습니다. 홈 스타일은 모든 홈 위젯에 공통 적용하며 플로팅 스타일과는 독립적입니다. 홈 위젯은 1×1 targetCell과 32dp 최소 리사이즈를 선언하나 실제 칸 크기·렌더링은 런처 정책에 따릅니다.

자동·수동 성공 조회는 응답 수신 시각을 저장하며 설정 변경·탭·실패가 현재 시각을 대신 저장하지 않습니다. 선택 한도가 누락되면 다른 한도로 조용히 교체하지 않습니다. 새 유효 데이터 저장 결과만 수동 성공 체크로 연결합니다. 요청 식별자로 오래된 작업 완료가 새 표시를 덮지 못하게 합니다. 반응 표시 만료는 단조 시계로 검사하고 Handler로 지웁니다. 홈·플로팅별 표시 여부·지속시간을 구분합니다. 프로세스 종료 시 홈 런처 이미지의 즉시 제거는 보장하지 않습니다. 정확한 알람 권한은 추가하지 않습니다.

앱 진입·복귀는 로컬 암호화 세션과 표시만 복구하며 조회하지 않습니다. 최초 로그인·명시적 조회·활성화된 주기 작업만 네트워크 조회를 수행합니다.

자동 조회 간격은 15~10080분 정수로 직접 설정하고 홈·플로팅이 하나의 JobScheduler 예약을 공유합니다. 잘못된 입력은 저장하지 않으며 기존 저장값이 범위 밖이거나 자료형이 잘못되면 15분 기본값을 사용합니다. 설정 저장은 예약을 조정할 뿐 즉시 사용량을 조회하지 않습니다. 정확한 알람·별도 폴링·새 권한을 추가하지 않으며 OS의 실행 지연은 계속 적용됩니다.

## 선택적 플로팅 표시

사용자가 허용한 TYPE_APPLICATION_OVERLAY 창에 저장된 사용량만 그립니다. 다른 앱의 화면·텍스트·입력·알림·세션을 읽거나 캡처하지 않습니다. 창은 포커스를 받지 않으며 창 영역의 탭·끌기·길게 누르기만 처리합니다. 표시 중에는 다른 앱 일부를 가릴 수 있으므로 필요하면 이동하거나 닫을 수 있습니다.

탭은 기존 수동 조회 경로를 사용합니다. 끌기·600ms 길게 누르기·표시·크기/스타일 변경은 사용량을 조회하지 않습니다. 홈 위젯의 3회 탭 중 첫 탭만 조회를 요청하고 나머지는 중복 요청을 억제합니다. 플로팅은 기존 저장소 변경을 관찰해 다시 그리며 별도 폴링·중계 서버를 추가하지 않습니다.

화면 꺼짐·잠금 시 창을 제거합니다. 0.6.4는 화면 복귀·잠금 해제 신호 뒤 단조 시계 기준 최대 15초 동안 500ms 간격으로 표시 가능 여부를 재확인합니다. 매번 화면의 interactive 상태와 Keyguard를 확인하며 잠금 상태를 무시해서 표시하지 않습니다. 성공하거나 화면이 다시 꺼지거나 세션이 종료되면 재시도를 중단합니다. 조회 효과 타이머와 복귀 재시도를 분리하고, 재시도에서 네트워크 요청·정확한 알람·자동 조회 예약 반복을 만들지 않습니다. 실행 중 세션의 일시 숨김은 자동 조회 예약을 취소하거나 주기를 다시 시작하지 않습니다.

사용자가 닫거나 권한을 철회하면 종료하며, 홈 위젯도 없을 때 자동 조회 예약을 취소합니다. 시스템이 프로세스를 종료한 경우에 한해 Android의 기존 서비스 재생성 경로로 복구를 시도합니다. 사용자가 닫은 위젯이나 재부팅 후에는 자동으로 열지 않습니다. 다른 앱이나 시스템의 오버레이 차단·강제 종료·제조사 절전 제한을 우회하지 않습니다. 한 번의 복귀 시도만 하던 경로와 프로세스 종료 후 복구되지 않던 경로를 보완하는 변경이며, 실제 사용자 One UI 현상의 원인이 이 경로였음을 입증한 것은 아닙니다.

0.6.3은 플로팅 창, 0.6.5는 일반 메인 화면에서 FLAG_SECURE를 제거합니다. 이 플래그는 Android의 창 캡처 보호 기능이며 이전 설정은 화면 캡처 제한의 원인이 될 수 있었습니다. **앱과 위젯의 사용량·날짜는 스크린샷·화면 녹화에 포함될 수 있습니다.** 공유 전에 확인하세요. 다른 앱·OS·관리자 정책의 캡처 보호를 우회하지 않습니다. [Android FLAG_SECURE](https://developer.android.com/reference/android/view/WindowManager.LayoutParams#FLAG_SECURE)

알림에 토큰·계정 ID·사용량 원문을 넣지 않습니다. 화면/권한 수신자·설정 리스너·창은 종료 시 정리합니다. 실제 S25 Ultra에서 캡처·녹화·표시 권한·잠금 복귀·회전·다른 앱과의 터치 상호작용은 미검증입니다.

## 절전 상태 안내와 로컬 진단

0.5.2는 시스템이 제공하는 절전 모드·배터리 최적화 예외·백그라운드 제한·예약 작업 상태를 읽어 안내합니다. 지원되지 않거나 읽지 못한 상태를 제한 없음으로 추정하지 않습니다. 삼성 절전·초절전 앱 목록을 직접 읽거나 수정하는 기능은 없습니다.

자동 작업의 최근 시도 시각·결과·중지 사유·위젯 게시 결과는 앱 전용 로컬 메타데이터입니다. 토큰·인증 코드·HTTP 헤더/본문·원본 오류 로그를 진단 기록으로 저장하거나 업로드하지 않습니다. 게시 성공은 갱신을 Android에 전달했다는 의미이며 실제 런처 화면이 바뀌었음을 확인한 것이 아닙니다.

배터리 예외 안내는 일반 시스템 설정 화면을 엽니다. 사용자가 폰에서 직접 선택하며 앱이 설정을 바꾸거나 예외를 강제 요청하지 않습니다. 이 진단·설정 안내 자체에는 별도 권한·알람·서비스·알림·사용량 요청이 없습니다. 0.6.0의 선택적 플로팅 서비스는 위 절에서 별도로 설명합니다. 상태 창과 앱 진입 자체는 네트워크 조회를 시작하지 않습니다.

배터리 최적화 예외는 모든 전력·네트워크 제한의 해제를 뜻하지 않습니다. 자동 조회는 계속 JobScheduler를 사용하며 OS·제조사 정책에 따라 늦어지거나 실행되지 않을 수 있습니다. 설정 안내는 사용자 S25 Ultra의 원인 진단이나 실기기 해결 검증이 아닙니다. [Android Doze·예외 범위](https://developer.android.com/training/monitoring-device-state/doze-standby) · [Android 전력 제한](https://developer.android.com/topic/performance/power/power-details)

## 검증 범위와 미검증

0.6.6의 현재 빌드·회귀 검사 결과는 [검사 기록](TEST-RESULTS.txt)에 별도로 기록합니다. 인증·지연 통신은 가짜 HTTPS/저장소, 플로팅과 서비스 수명주기는 모의 Android 상태·시계·창 오류를 사용합니다. 이번 버전에서는 에뮬레이터·실제 화면 잠금·시스템 프로세스 종료·S25 Ultra·실계정 실행을 검증하지 않았습니다.

아래 수치는 **이전 0.6.3 APK의 검사 기록**이며 현재 버전의 새 동작을 실행 검증한 결과가 아닙니다.

**0.6.3 APK의 Windows 전체 빌드·정렬·동일 서명과 로컬 검사 15,026개**, 별도 아이콘 49개·XML 18개·리소스 검사를 통과했습니다. 플로팅 캡처 플래그와 메인 보호 유지에 관한 소스 계약 6개를 추가했고 기존 검사도 다시 실행했습니다. 새 검사는 수정 전 실패·수정 후 통과를 확인했습니다. [검사 기록](TEST-RESULTS.txt)

별도 **Android 15(API 35) 격리 Pixel 8 에뮬레이터 실행 검사 99개**를 0.6.3 APK에서 통과했습니다. 실제 부착된 플로팅 창의 FLAG_SECURE 제거, 비포커스·창 밖 터치 전달·화면 좌표 설정 유지, 잠금 복귀 콜백 후 보호 플래그 없음, 메인 화면의 FLAG_SECURE 유지를 검사했습니다. 기존 미리보기·불투명도·간격·플로팅 검사 91개도 포함해 다시 통과했습니다. 네트워크를 끄고 합성 데이터만 사용했으며, 실제 OS 스크린샷·화면 녹화 성공 검사는 아닙니다.

0.6.3 검사 첫 실행은 32개 통과 후 기존 Activity 진입의 4초 대기에서 실패했습니다. 호스트·에뮬레이터 지연을 확인하고 테스트의 Activity 대기만 15초, 전체 상한을 150초로 늘린 뒤 같은 APK와 같은 검사 조건으로 99개를 통과했습니다. 제품 APK는 바꾸지 않았습니다. 0.6.3 릴리스의 별도 시각 검토는 하지 않았으며 0.6.2의 DecorView PNG 검토도 OS 캡처의 근거가 아닙니다.

표시 권한은 테스트용 appops로 부여하고 화면 꺼짐·복귀는 수신자를 직접 호출했습니다. 실제 권한 UI·잠금/해제·홈 런처 3회 탭·설치된 홈 위젯의 실측 크기·실제 47분 네트워크 주기는 미검증입니다.

실계정 브라우저 로그인/토큰 발급·회전, 자격증명의 실제 Android Keystore 저장, 물리 기기의 APK 설치, Galaxy S25 Ultra/One UI 격자·플로팅·실제 손가락 터치·권한 복귀·잠금·회전·재부팅·절전·장시간 동작은 미검증입니다. 로컬 합성 소켓 통신은 실제 폰 브라우저 콜백 시험이 아니며 대체 Android 객체 검사는 위 에뮬레이터 검사와 별개입니다. 연결된 물리 Android 기기가 없어 실기기 검증을 수행하지 못했습니다. 에뮬레이터와 로컬 진단 검사는 사용자의 자동 조회 문제 원인이나 실기기 해결을 입증하지 않습니다.

기본 설계 참고: [OpenAI 인증](https://learn.chatgpt.com/docs/auth), [Android foreground service](https://developer.android.com/develop/background-work/services/fgs/service-types), [Android 위젯 레이아웃](https://developer.android.com/develop/ui/views/appwidgets/layouts).

수동 조회 시작 조건 참고: [Android 백그라운드 foreground service 시작 예외](https://developer.android.com/develop/background-work/services/fgs/restrictions-bg-start). 사용자의 위젯 조작이 명시된 예외에 해당하는 경로를 사용합니다. 이 문서는 개별 제조사·강제 중지·네트워크 상태에서 성공을 보증하지 않습니다.
